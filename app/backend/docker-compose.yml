services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: centrocontrol
      POSTGRES_PASSWORD: centrocontrol
      POSTGRES_DB: centrocontrol
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data: